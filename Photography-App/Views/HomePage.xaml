<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Photography_App.Views.HomePage"
             xmlns:tabs="http://sharpnado.com"
             xmlns:controls="clr-namespace:Photography_App.Controls"
             Title="{Binding Title}"
             xmlns:local="clr-namespace:Photography_App.ViewModels"  
             xmlns:model="clr-namespace:Photography_App.Models"  
             x:Name="HomePage">

    <ContentPage.ToolbarItems>
        <!--<ToolbarItem Command="{Binding AddItemCommand}" IconImageSource="red_dwarf_lister.jpg"  />-->
    </ContentPage.ToolbarItems>

    <Grid>
        <ScrollView Orientation="Vertical" >
            <StackLayout StyleClass="MainLayout">
                <Label Text="Test"/>
                <Label Text="Test"/>
            </StackLayout>
        </ScrollView>

        <StackLayout ClassId="Menu" VerticalOptions="End">
            <controls:Menu></controls:Menu>
        </StackLayout>

    </Grid>

</ContentPage>
